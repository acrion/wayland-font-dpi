#!/usr/bin/env bash
# Copyright (c) 2025 acrion innovations GmbH
# Authors: Stefan Zipproth, s.zipproth@acrion.ch
#
# This file is part of wayland-font-dpi, see https://github.com/acrion/wayland-font-dpi
#
# wayland-font-dpi is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# wayland-font-dpi is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with wayland-font-dpi. If not, see <https://www.gnu.org/licenses/>.

# Session helper for Wayland Font DPI
# This script is executed by wayland-font-dpi-session.service (in user context).
# It reads the file /var/cache/wayland-display-info/display-info which is
# maintained by the wayland-display-info service.
# It applies the per-user GTK scaling.

set -eu

CACHE_DIR="/var/cache/wayland-display-info"
CACHE_FILE="$CACHE_DIR/display-info"
REFERENCE_DPI=96
DESKTOP_WAIT_SECONDS=30

usable_dpi() {
    [[ $1 =~ ^[0-9]+(\.[0-9]+)?$ ]] && (( $(bc -l <<< "$1 > 0") ))
}

apply_scale() {
    local best_line dpi width height scale

    # Choose line with highest pixel count (width×height)
    if [[ -r $CACHE_FILE ]]; then
        best_line=$(awk '{px=$3*$4; if(px>max){max=px; l=$0}} END{print l}' "$CACHE_FILE")
    fi

    # display-info is read once, because wayland-display-info can replace it
    # between two reads. It is empty while no output is enabled, for instance
    # once every output has been switched off; there is nothing to scale for
    # then, so the factor applied last stays in effect.
    if [[ -z ${best_line:-} ]]; then
        echo "<6>display-info lists no enabled output – keeping current scaling"
        return 0
    fi

    read -r _ dpi width height <<< "$best_line"

    # Releases of wayland-display-info up to 1.0.8 write "inf" for an output
    # that reports a physical height of 0 mm. Nothing can be scaled for such a
    # line, and ending the daemon would only cause systemd to restart it every
    # three seconds.
    if ! usable_dpi "$dpi"; then
        echo "<4>display-info has no usable DPI value – keeping current scaling (line: $best_line)"
        return 0
    fi

    scale=$(bc -l <<< "scale=3; $dpi / $REFERENCE_DPI")
    gsettings set org.gnome.desktop.interface text-scaling-factor "$scale" 2>/dev/null || true
    echo "<5>Applied GTK text-scaling factor $scale (line: $best_line)"
}

# The desktop associated with the session visible on screen, queried from
# logind. One systemd user manager is common to every session of a user, and a
# desktop writes its own values into it without ever taking them back: a second
# one started from a tty, or one that has ended since, leaves its
# XDG_CURRENT_DESKTOP behind in that space. A daemon that trusts that variable
# then works for a desktop nobody is observing. logind knows which session
# holds the display, and that session names its own desktop.
session_desktop() {
    local id
    id=$(loginctl show-user "$(id -u)" --property=Display --value 2>/dev/null) || return 1
    [[ -n $id ]] || return 1
    loginctl show-session "$id" --property=Desktop --value 2>/dev/null
}

# Prints the name of the desktop, favouring the value provided by logind over
# the shared user environment. The service may start prior to logind
# establishing a display session, and prior to the compositor importing the
# variable, thus both sources are asked for up to DESKTOP_WAIT_SECONDS. A
# compositor that sets neither gets an empty name after that time has passed.
current_desktop() {
    local _ value
    for (( _ = 0; _ < DESKTOP_WAIT_SECONDS; _++ )); do
        value=$(session_desktop) || true
        if [[ -n ${value:-} ]]; then
            echo "$value"
            return 0
        fi
        if [[ -n ${XDG_CURRENT_DESKTOP:-} ]]; then
            echo "$XDG_CURRENT_DESKTOP"
            return 0
        fi
        value=$(systemctl --user show-environment 2>/dev/null | sed -n 's/^XDG_CURRENT_DESKTOP=//p') || true
        if [[ -n $value ]]; then
            echo "$value"
            return 0
        fi
        sleep 1
    done
}

# XDG_CURRENT_DESKTOP is a colon-separated list, and logind reports the desktop
# in whatever case the session file wrote it, so this is neither an equality nor
# a comparison that may care about case.
is_cosmic() {
    local IFS=: name
    for name in ${1:-}; do
        [[ ${name^^} == COSMIC ]] && return 0
    done
    return 1
}

main() {
    local desktop

    # Don't run for display manager users
    [[ ${USER:-} =~ ^(lightdm|gdm|sddm)$ ]] && exit 0

    # Said rather than done silently: an exit 0 leaves no trace, and
    # Restart=on-failure does not catch it either, so a daemon that stops here
    # for the wrong reason looks exactly like one that was never wanted.
    desktop=$(current_desktop)
    if is_cosmic "$desktop"; then
        echo "<5>$desktop scales by itself – nothing to do"
        exit 0
    fi

    # Wait until wayland-display-info produced the cache file
    until [[ -r $CACHE_FILE ]]; do sleep 1; done
    apply_scale

    # Monitor file for changes
    while read -r _ _ file; do
        [[ "$file" == "display-info" ]] || continue
        sleep 0.1
        echo "<6>display-info changed – reprocessing (session)..."
        apply_scale
    done < <(
        inotifywait -q -m -e close_write,create,move --format '%w %e %f' "$CACHE_DIR"
    )
}

# Sourcing this file, as the tests do, defines the functions without starting
# the daemon.
if [[ ${BASH_SOURCE[0]} == "$0" ]]; then
    main
fi
